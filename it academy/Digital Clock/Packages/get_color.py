import random

def get_color():
    #return random color from 32 to 38
    while True:
        b = random.randrange(32, 38, 1)
        yield b
        yield b
        yield b
        yield b
        yield b
        yield b
        yield b
        yield b
        yield b
        yield b